<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.dao.LectureMapper">

	<select id="getCheckedLecture" resultType="String">
		SELECT lecturenum
		FROM userlectureinfo
		WHERE userid = #{userId} AND coursenum = #{courseNumber}
	</select>

	<select id="getLastedLecture" resultType="Lecture">
		SELECT *
		FROM lecture
		WHERE lecno = (
			SELECT lecturenum
			FROM userlectureinfo
			WHERE userid = #{userId} AND coursenum = #{courseNumber}
			ORDER BY lecturenum ASC
			LIMIT 1
		)
	</select>

	
</mapper>